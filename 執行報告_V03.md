# 執行報告 V03 — 第2頁文字雲改題

- 時間：2026-06-23
- 交付物：`index.html` 第2頁（即時文字雲互動頁）
- 任務：把第2頁文字雲的題目改成「你認為使用 HTML 取代 Markdown 的理由為何？」
- 模式：正式執行

## 背景
第2頁原本就是一個 Firebase 即時文字雲（輸入框 + 文字雲 + 熱門榜），原題目為「破冰：你平常都請 AI 用什麼格式輸出？」。經與 tino 確認，採「改現有題目」而非「新增第二個」，並沿用現有 Firebase 專案、另開新 collection 隔離資料。

## 做了什麼（index.html 共 4 處）
1. 題目 `<h2>`：改為「你認為使用 **HTML 取代 Markdown** 的理由為何？」
2. 輸入框 placeholder：`Markdown / 表格 / HTML…` → `資訊密度高 / 可互動 / 排版漂亮…`（引導寫關鍵詞，呼應第5頁五大理由）
3. 輸入框 `maxlength`：20 → 30（理由通常比格式詞長）
4. Firebase collection：`htmlpres_wordcloud` → `htmlpres_reason`（沿用 teacherstudy-109ef 連線，新集合不與舊「格式」答案混）

## 驗證（preview reload 後 eval 核對）
- 新題目、新 placeholder、maxlength=30 皆正確上線。
- 文字雲畫布存在、WordCloud 函式庫載入正常。
- Firebase onSnapshot 已連上 `htmlpres_reason`，初始 0 筆（乾淨集合、資料隔離成功）。
- 即時送出／文字雲渲染沿用原本可運作之程式，僅替換題目與集合名，功能不受影響。

## 警告 / 待辦
- 即時互動最終效果建議現場實測：在第2頁送出 1–2 筆理由，確認文字雲即時跳出。
- 尚未 git commit（本輪累積：3 張背景圖、第2頁改題、V02/V03 報告）。
- 舊 collection `htmlpres_wordcloud` 仍保留歷史資料，不影響新題目；如不再需要可自行於 Firebase 主控台清理。
